# Frontend

Vue 3 + Vite front end for the flower recognition system.

## Development

```powershell
cd E:\Flower_Project\frontend
npm install
npm run dev
```

Open:

```text
http://127.0.0.1:5173
```

The app proxies `/api` requests to the FastAPI backend at `http://127.0.0.1:8000`.

