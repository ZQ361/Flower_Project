# 毕业设计项目总结

## 一、项目结构

本项目是一个面向花卉图片识别与科普问答的完整 Web 应用，整体采用前端、后端、模型服务分离的结构。系统核心流程为：用户在前端上传花卉图片，后端接收请求并调用 PyTorch 模型完成分类识别，同时结合花卉资料库提供识别结果、科普信息、收藏、历史记录和智能问答能力。

```text
Flower_Project/
├── backend/                         # FastAPI 后端服务
│   ├── app/
│   │   ├── core/                    # 配置、鉴权和安全工具
│   │   ├── data/                    # 花卉中文资料 JSON
│   │   ├── db/                      # SQLAlchemy 模型、会话和数据访问
│   │   ├── routers/                 # API 路由
│   │   ├── services/                # 业务逻辑服务
│   │   └── main.py                  # 后端应用入口
│   └── requirements.txt             # 后端依赖
├── frontend/                        # Vue 3 + Vite 前端
│   ├── public/                      # 静态资源
│   ├── src/
│   │   ├── components/              # 上传、结果、科普库、登录等组件
│   │   ├── lib/                     # API 请求和本地存储工具
│   │   ├── styles/                  # 前端样式
│   │   └── App.vue                  # 前端主组件
│   └── package.json                 # 前端依赖与脚本
├── model_service/                   # 模型训练、评估和推理模块
│   ├── checkpoints/                 # 训练得到的模型权重
│   ├── data/                        # Flowers102 数据集
│   ├── log/                         # 训练日志、实验结果和可视化图
│   ├── utils/                       # 数据集、模型和训练工具
│   ├── config.py                    # 模型与训练参数配置
│   ├── main_train.py                # 主训练脚本
│   ├── main_train_no_aug.py         # 无数据增强消融实验
│   ├── main_train_head_only.py      # 只训练分类头消融实验
│   ├── inference.py                 # 推理接口
│   └── analyze_confusion.py         # 混淆矩阵分析
├── README.md                        # 项目说明
└── 毕业设计项目总结.md              # 项目总结文档
```

项目主要模块如下：

| 模块 | 技术栈 | 主要职责 |
| --- | --- | --- |
| 前端 | Vue 3、Vite、JavaScript | 图片上传、识别结果展示、花卉科普库、用户登录、收藏、历史记录、问答交互 |
| 后端 | FastAPI、SQLAlchemy、SQLite、JWT | API 服务、用户认证、数据持久化、识别历史、收藏管理、问答会话管理 |
| 模型服务 | PyTorch、torchvision | Flowers102 花卉分类模型训练、评估、消融实验、模型推理 |

## 二、训练参数

本项目使用 Oxford Flowers102 数据集进行花卉分类实验，共包含 102 个花卉类别。训练脚本通过 `torchvision.datasets.Flowers102` 加载数据，并按官方划分使用 train、val、test 三个子集。

| 参数项 | 配置 |
| --- | --- |
| 数据集 | Oxford Flowers102 |
| 类别数 | 102 |
| 输入尺寸 | 224 x 224 |
| Batch Size | 32 |
| 随机种子 | 42 |
| 损失函数 | CrossEntropyLoss |
| 优化器 | Adam |
| 训练设备 | 自动选择 CUDA，若不可用则使用 CPU |
| DataLoader | `num_workers=2`，`pin_memory=True` |

主训练流程采用迁移学习和两阶段训练策略：

| 阶段 | 训练方式 | 最大轮数 | 学习率 | 说明 |
| --- | --- | ---: | ---: | --- |
| 阶段 1 | 冻结预训练特征提取层，只训练分类头 | 20 | 1e-3 | 快速适配 102 类花卉分类任务 |
| 阶段 2 | 解冻全部网络层进行全量微调 | 80 | 1e-5 | 在预训练特征基础上进一步提升分类效果 |

阶段 2 使用 `ReduceLROnPlateau` 学习率调度器，监控验证集准确率，`patience=5`，`factor=0.5`。训练函数中还设置 Early Stopping，当阶段 2 验证集准确率连续 8 个 epoch 没有提升时提前停止训练，并保存验证集准确率最高的模型权重。

训练集使用的数据增强包括：

| 增强方式 | 参数 |
| --- | --- |
| Resize | 224 |
| RandomResizedCrop | 224，scale=(0.8, 1.0) |
| RandomHorizontalFlip | p=0.5 |
| RandomRotation | 15 度 |
| ColorJitter | brightness=0.2，contrast=0.2，saturation=0.2 |
| GaussianBlur | kernel_size=3，p=0.3 |
| Normalize | ImageNet mean/std |

验证集和测试集不使用随机增强，仅使用 Resize、CenterCrop、ToTensor 和 ImageNet 标准化，以保证评估结果稳定。

## 三、模型结果

### 1. 不同主干网络对比

项目分别训练并对比了 EfficientNet-B0、MobileNetV2 和 ResNet18 三种预训练模型。三种模型均将最后分类层替换为 102 类输出，并采用相同的数据集划分和两阶段迁移学习策略。

| 模型 | 阶段 1 最佳验证准确率 | 阶段 2 最佳验证准确率 | 测试集准确率 | 结果文件 |
| --- | ---: | ---: | ---: | --- |
| EfficientNet-B0 | 84.51% | 89.51% | 87.35% | `efficientnet_b0_flowers102_final.pth` |
| MobileNetV2 | 80.78% | 87.94% | 84.70% | `mobilenet_v2_flowers102_final.pth` |
| ResNet18 | 未单独记录 | 89.22% | 86.94% | `resnet18_flowers102_final.pth` |

从实验结果看，EfficientNet-B0 在测试集上取得 87.35% 的最高准确率，同时模型权重文件约 16 MB，综合精度和部署成本表现最好，因此最终系统默认使用 EfficientNet-B0 作为花卉识别模型。

### 2. EfficientNet-B0 消融实验

为验证训练策略的有效性，项目对 EfficientNet-B0 进行了消融实验，对比完整方法、去除数据增强和只训练分类头三种方案。

| 实验方法 | 验证集准确率 | 测试集准确率 | 停止轮次 | Checkpoint |
| --- | ---: | ---: | ---: | --- |
| 完整方法 | 89.51% | 87.35% | 43 | stage2 |
| 无数据增强 | 89.41% | 86.01% | 29 | stage2 |
| 只训练分类头 | 85.78% | 82.60% | 56 | stage2 |

消融结果说明：

- 去除数据增强后，验证集准确率变化较小，但测试集准确率从 87.35% 降至 86.01%，说明随机裁剪、翻转、旋转和颜色扰动能提升模型泛化能力。
- 只训练分类头时，测试集准确率降至 82.60%，明显低于完整两阶段训练，说明阶段 2 全量微调对花卉细粒度识别非常关键。
- 完整方法在验证集和测试集上都取得最佳表现，证明“预训练模型 + 数据增强 + 分类头预训练 + 全量微调”的训练方案更适合本项目。

### 3. 混淆类别分析

项目通过混淆矩阵进一步分析 EfficientNet-B0 的错误分类情况。Top 混淆类别主要集中在外观相似、颜色接近或花型相近的类别之间，例如：

| 真实类别 | 易混预测类别 | 错误次数 | 错误占该类比例 |
| --- | --- | ---: | ---: |
| petunia | hibiscus | 28 | 11.76% |
| watercress | hibiscus | 16 | 9.76% |
| petunia | azalea | 12 | 5.04% |
| rose | cyclamen | 11 | 7.28% |
| snapdragon | sweet pea | 9 | 13.43% |

这说明模型对多数花卉类别已有较好的区分能力，但在花瓣形态、颜色和拍摄角度较接近的类别上仍然存在误判。

## 四、实验结论

本项目完成了一个从模型训练到 Web 应用部署的花卉识别与科普系统。模型部分基于 PyTorch 和 torchvision 进行迁移学习，系统部分基于 Vue 3 和 FastAPI 实现用户交互、接口服务和数据管理，整体具备较完整的毕业设计项目形态。

实验结果表明，EfficientNet-B0 在 Flowers102 数据集上取得了最好的综合表现，测试集准确率达到 87.35%，优于 MobileNetV2 的 84.70% 和 ResNet18 的 86.94%。在模型大小方面，EfficientNet-B0 权重文件约 16 MB，明显小于 ResNet18，适合作为本系统的默认部署模型。

两阶段训练策略对模型性能提升明显。阶段 1 只训练分类头可以让模型快速适配花卉分类任务，阶段 2 解冻全部参数后，模型能够进一步学习花卉数据集中的细粒度特征。EfficientNet-B0 从阶段 1 的 84.51% 验证准确率提升到阶段 2 的 89.51%，证明全量微调是提升最终识别效果的重要步骤。

数据增强对泛化能力也有积极作用。无数据增强实验的测试集准确率为 86.01%，低于完整方法的 87.35%。这说明在花卉图像存在姿态、光照、背景和拍摄角度变化的情况下，随机裁剪、翻转、旋转、颜色扰动和模糊增强有助于提高模型在未见样本上的表现。

从混淆矩阵结果看，模型错误主要出现在外观相似的花卉类别之间，例如 petunia、hibiscus、azalea 等类别容易互相混淆。后续可以通过增加细粒度局部特征学习、引入注意力机制、扩充相似类别样本或使用更强的视觉主干网络来进一步提升识别准确率。

综上，本项目实现了花卉识别、花卉科普、用户系统、收藏历史和智能问答等功能，并通过多模型对比和消融实验证明了所选训练方案的有效性。最终采用 EfficientNet-B0 作为识别模型，在准确率、模型体积和部署便利性之间取得了较好的平衡，能够满足毕业设计中对完整性、实用性和实验分析的要求。
